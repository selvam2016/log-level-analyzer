# Log Level Analyzer

A Python script that reads a log file and counts INFO, WARNING, and ERROR occurrences.

## How to run
python log_analyzer.py yourlogfile.txt

## Output
Prints log level counts and saves results to output.txt
